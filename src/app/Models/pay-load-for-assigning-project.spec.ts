import { PayLoadForAssigningProject } from './pay-load-for-assigning-project';

describe('PayLoadForAssigningProject', () => {
  it('should create an instance', () => {
    expect(new PayLoadForAssigningProject()).toBeTruthy();
  });
});
