import { UserRegisterationForm } from './user-registeration-form';

describe('UserRegisterationForm', () => {
  it('should create an instance', () => {
    expect(new UserRegisterationForm()).toBeTruthy();
  });
});
